"""Lint and compliance tests that scan repository content for required tokens.

The reviewer-envelope lint test ships in Wave 1 with a per-wave scope list
(see ``test_reviewer_envelope_required.py``). Subsequent waves extend the
list inline rather than introducing new lint files.
"""
