"""Markdown fixtures for ``tests/scripts/orchestrator/test_envelope_parser.py``.

Each ``.md`` file in this directory simulates a single reviewer agent's full
output (prose body + optional ```json envelope`` final block) per the
discriminator semantics defined in §4.3 of the epic plan.
"""
